pandas>=2.0.0
requests>=2.34.2
lxml>=6.1.1
pytest>=9.1.1
pytest-cov>=7.1.0
rapidfuzz>=3.14.5
python-dotenv>=1.2.2
pyarrow>=25.0.0
# R5 foundation deps
PyYAML>=6.0.3
networkx>=3.6.1; python_version >= '3.11'
networkx>=3.0; python_version < '3.11'
# ACT/ACUDEN PDF extraction harness
pdfplumber>=0.11.10
# SBA disaster-loan workbook importer
openpyxl>=3.1.5
# Shared maintenance core from thehub-pr, resolved via a local editable path
# instead of a pinned commit SHA: edit the package once and every consumer picks
# it up with no per-repo bump. Requires thehub-pr checked out as a sibling of this
# repo (path is relative to the repo root, where uv/pip runs).
-e ../thehub-pr/packages/prii_maintenance
